<UserControl x:Class="CryptexScriptInspector.Controls.InstructionArgumentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CryptexScriptInspector.Controls"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="ucInstructionArgumentControl">

    <StackPanel Orientation="Vertical" Margin="5 0">
        <TextBlock x:Name="tbTitle" Text="{Binding Path=Title, ElementName=ucInstructionArgumentControl}" />
        <Separator Margin="0 0 0 2" />
        <RadioButton x:Name="rbNone" Checked="RbNone_OnChecked" Content="None" IsChecked="True" />
        <RadioButton x:Name="rbMemory" Checked="RbMemory_OnChecked" Content="Memory" />
        <RadioButton x:Name="rbHex" Checked="RbHex_OnChecked" Content="Hex" />
        <RadioButton x:Name="rbDecimal" Checked="RbDecimal_OnChecked" Content="Decimal" />
        <RadioButton x:Name="rbLabel" Checked="RbLabel_OnChecked" Content="Label" />
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0 3 0 0">
            <TextBlock Text="Content" VerticalAlignment="Center" Margin="0 0 5 0"/>
            <TextBox x:Name="tbContent" MinWidth="100px" MaxHeight="45px" materialDesign:TextFieldAssist.PrefixText="$" TextChanged="TbContent_OnTextChanged" IsEnabled="False">
                <TextBox.Text>
                    <Binding Path="ChunkName"
                             UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:InstructionArgumentContentValidationRule ValidatesOnTargetUpdated="True" />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
    </StackPanel>

</UserControl>